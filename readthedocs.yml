# .readthedocs.yml
version: 0.1

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.12.9"

python:
  install:
    - method: pip
      requirements: docs/requirements.txt
